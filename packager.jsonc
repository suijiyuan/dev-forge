{
  // VS Code Windows 发行包配置。
  "vscode": {
    // 必填。目标 VS Code 的完整稳定版本号，格式必须为 major.minor.patch。
    // 此版本既用于下载 VS Code，也用于筛选兼容的扩展版本。
    // 支持示例："1.95.3"、"1.100.0"。
    // 不支持："latest"、"1.95"、Insiders 版本或版本范围。
    "version": "1.132.0",

    // 可选，默认值："system"。Windows 发行包类型：
    // - "system"：System Installer，输出 .exe，通常用于整机安装；
    // - "user"：User Installer，输出 .exe，安装到当前用户目录；
    // - "archive"：免安装 ZIP；生成的 install.ps1 会自动解压它。
    "package": "user",

    // 可选，默认值："x64"。目标 Windows CPU 架构：
    // - "x64"：Intel/AMD 64 位 Windows；
    // - "arm64"：ARM 64 位 Windows。
    // 此项也用于优先选择同架构 VSIX；没有专用构建时选择通用 VSIX。
    "arch": "x64"
  },

  // 可选，默认值：[]。需要下载的 Visual Studio Marketplace 扩展 ID 列表。
  // 每项必须使用 "publisher.name" 格式，可通过 `code --list-extensions` 获取。
  // ID 不区分大小写；程序会转成小写并自动去重。
  // 对每个 ID，程序会自动选择：非预发布、兼容目标 VS Code、兼容目标
  // Windows 架构的最高版本。当前不支持固定扩展版本。
  // 注意：只下载这里明确列出的扩展，不自动展开 Extension Pack 或依赖。
  "extensions": [
    "christian-kohler.npm-intellisense",
    "christian-kohler.path-intellisense",
    "dbaeumer.vscode-eslint",
    "eamodio.gitlens",
    "esbenp.prettier-vscode",
    "formulahendry.auto-close-tag",
    "formulahendry.auto-complete-tag",
    "formulahendry.auto-rename-tag",
    "humao.rest-client",
    "ms-azuretools.vscode-docker",
    "ms-python.debugpy",
    "ms-python.python",
    "ms-python.vscode-pylance",
    "ms-python.vscode-python-envs",
    "ms-vscode-remote.remote-ssh",
    "ms-vscode-remote.remote-ssh-edit",
    "ms-vscode.remote-explorer",
    "mtxr.sqltools",
    "mtxr.sqltools-driver-mysql",
    "octref.vetur",
    "redhat.java",
    "streetsidesoftware.code-spell-checker",
    "vmware.vscode-spring-boot",
    "vscjava.vscode-gradle",
    "vscjava.vscode-java-debug",
    "vscjava.vscode-java-dependency",
    "vscjava.vscode-java-pack",
    "vscjava.vscode-java-test",
    "vscjava.vscode-lombok",
    "vscjava.vscode-maven",
    "vue.volar",
    "xyz.plsql-language",
    "yangbaopan.vscode-java-ibatisx"
  ],

  // 可选，默认值："auto"。要导出的 settings.json：
  // - "auto"：按当前操作系统自动查找 VS Code 用户 settings.json；
  // - 其他字符串：settings.json 的绝对路径、~ 路径，或相对于本配置文件的路径。
  // 文件必须存在。程序原样复制，不解析或修改 JSON/JSONC 内容。
  "settings": "auto",

  // 可选，默认值："dist"。产物输出目录。
  // 支持绝对路径、~ 路径，或相对于本配置文件的路径；不存在时自动创建。
  // 输出文件夹和 ZIP 名称由 VS Code 版本及架构自动生成。
  // 如果同名文件夹或 ZIP 已存在，程序会停止，避免覆盖已有产物。
  "output_dir": "dist"
}
